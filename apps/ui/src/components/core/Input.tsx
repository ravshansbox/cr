import { styled } from '@/styled-system/jsx';

export const Input = styled('input', {
  base: {
    borderRadius: 'md',
    borderWidth: '1px',
    borderColor: 'slate.300',
    paddingX: '2',
    paddingY: '1',
  },
});
